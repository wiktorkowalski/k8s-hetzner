apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Playground cluster - safe to test CNPG features
  - playground-cluster.yaml

  # Production example cluster (commented out)
  # - example-cluster.yaml

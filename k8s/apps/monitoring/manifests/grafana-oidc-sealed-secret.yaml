# PLACEHOLDER - Configure Authentik first:
# 1. In Authentik, create OAuth2/OIDC Provider:
#    - Name: Grafana
#    - Redirect URIs: https://grafana.k8s.vicio.ovh/login/generic_oauth
#    - Scopes: openid, profile, email, groups
# 2. Create application in Authentik
# 3. Install kubeseal: brew install kubeseal
# 4. kubectl create secret generic grafana-oidc \
#      --from-literal=OIDC_CLIENT_ID="<from-authentik>" \
#      --from-literal=OIDC_CLIENT_SECRET="<from-authentik>" \
#      --namespace monitoring --dry-run=client -o yaml | \
#      kubeseal --controller-namespace kube-system --controller-name sealed-secrets-controller \
#      -o yaml > k8s/apps/monitoring/manifests/grafana-oidc-sealed-secret.yaml

# Temporary plain secret for testing - REPLACE WITH SEALEDSECRET BEFORE PRODUCTION
apiVersion: v1
kind: Secret
metadata:
  name: grafana-oidc
  namespace: monitoring
type: Opaque
stringData:
  OIDC_CLIENT_ID: "placeholder-client-id"
  OIDC_CLIENT_SECRET: "placeholder-secret"

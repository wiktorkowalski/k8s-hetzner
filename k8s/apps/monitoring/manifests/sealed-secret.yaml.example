# SealedSecret for Grafana admin password
#
# To create this secret:
# 1. Create a regular secret:
#    kubectl create secret generic grafana-admin \
#      --from-literal=admin-password="your-secure-password" \
#      --namespace monitoring \
#      --dry-run=client -o yaml > /tmp/grafana-secret.yaml
#
# 2. Seal it:
#    kubeseal -f /tmp/grafana-secret.yaml -w k8s/apps/monitoring/manifests/sealed-secret.yaml
#
# 3. Commit to git
# 4. Delete temp: rm /tmp/grafana-secret.yaml
---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: grafana-admin
  namespace: monitoring
spec:
  encryptedData:
    admin-password: <ENCRYPTED_VALUE_HERE>
  template:
    metadata:
      name: grafana-admin
      namespace: monitoring

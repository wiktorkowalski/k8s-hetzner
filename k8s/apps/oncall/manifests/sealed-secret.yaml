# PLACEHOLDER - Install kubeseal and run:
# kubectl create secret generic oncall-secrets \
#   --from-literal=SECRET_KEY="$(openssl rand -base64 32)" \
#   --from-literal=GRAFANA_API_KEY="YOUR_GRAFANA_API_KEY" \
#   --namespace oncall --dry-run=client -o yaml | \
#   kubeseal --controller-namespace kube-system --controller-name sealed-secrets-controller \
#   -o yaml > k8s/apps/oncall/manifests/sealed-secret.yaml

# Temporary plain secret for testing - REPLACE WITH SEALEDSECRET BEFORE PRODUCTION
apiVersion: v1
kind: Secret
metadata:
  name: oncall-secrets
  namespace: oncall
type: Opaque
stringData:
  SECRET_KEY: "temporary-change-me-before-production"
  GRAFANA_API_KEY: "temporary-placeholder"

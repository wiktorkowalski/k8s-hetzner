apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: oncall
  namespace: oncall
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`oncall.k8s.vicio.ovh`)
      kind: Rule
      services:
        - name: oncall
          port: 8080
  tls:
    certResolver: default
